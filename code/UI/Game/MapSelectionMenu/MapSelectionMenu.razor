@using TTT;
@using TTT.UI;

@namespace TTT.UI
@using Sandbox.UI;
@inherits Panel

<root>
    <div class="inner text-shadow">
        <div class="header">
            <div class="left">
                <span class="title">Map Vote</span>
                <span class="subtitle">Choose your next map</span>
            </div>
            <div class="right">
                <text class="countdown">@GameManager.Instance.State.TimeLeftFormatted</text>
            </div>
        </div>
        <div @ref="Maps" class="body">
            @foreach (var map in GameManager.Instance.MapVoteIdents)
            {
                <MapIcon Ident=@map />
            }
        </div>
    </div>
</root>

<style>
    MapSelectionMenu {
        position: absolute;
        left: -50px;
        right: -50px;
        top: -50px;
        bottom: -50px;
        pointer-events: all;

        .inner {
            margin: 150px 20%;
            flex-direction: column;
            flex-grow: 1;

            .header {
                flex-direction: row;

                .left {
                    flex-grow: 1;
                    flex-direction: column;

                    .title {
                        font-size: 50px;
                        font-weight: bold;
                    }

                    .subtitle {
                        font-size: 24px;
                        font-weight: bold;
                    }
                }

                .right {
                    .countdown {
                        font-size: 50px;
                        font-weight: bold;
                    }
                }
            }

            .body {
                margin: 50px;
                flex-wrap: wrap;
                overflow: scroll;
            }
        }
    }
</style>
