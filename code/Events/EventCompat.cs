namespace TTT;

public static class Event
{
	public static void Run( string name, params object[] args )
	{
		// Temporary compatibility shim while migrating away from the legacy direct event dispatcher.
	}
}
